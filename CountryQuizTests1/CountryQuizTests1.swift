//
//  CountryQuizTests1.swift
//  CountryQuizTests1
//
//  Created by iOS Developer on 06/09/25.
//

import XCTest
@testable import CountryQuiz

final class CountryQuizTests1: XCTestCase {
    
    /*
    var service: CountryService?
    var session: URLSession!
    
    override func setUp(){
        super.setUp()
        
        let config = URLSessionConfiguration.ephemeral
        config.protocolClasses = [MockURLProtocol.self]
        guard let session = URLSession(configuration: config) else { return }
        
        service = CountryService(session: session)
    }
    
    override class func tearDown() {
        service = nil
        session = nil
        super.tearDown()
    }
     */

}
